version: "3.8"
services:
  standalone:
    image: dgraph/standalone:v22.0.2
    # restart: always
    networks:
      - sdgservices
    volumes:
      - type: volume
        source: dgraph
        target: /dgraph
    deploy:
      placement:
        constraints:
          - node.hostname==${HOST_CONSTRAINT}
networks: 
  sdgservices: 
    external: true
 
volumes:
  dgraph:
    external: true
    name: dgraph
    