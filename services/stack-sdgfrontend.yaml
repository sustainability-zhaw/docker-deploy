version: "3.8"
services:
  frontend:
    image: ghcr.io/sustainability-zhaw/sustainability-dashboard:${IMAGETAG}
    networks:
      - sdgservices
    configs:
      - source: sdgweb_config
        target: /usr/share/caddy/config.json

networks: 
  sdgservices: 
    external: true

configs: 
  sdgweb_config:
    external: true
    name: ${CONFIGNAME}

